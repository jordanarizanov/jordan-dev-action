name: jordan-loci-insights
description: Binary analysis and function insights tool for code quality assessment
author: Jordan API
branding:
  icon: code
  color: blue
inputs:
  mode:
    description: Mode to run
    required: true
  requirements-file:
    description: Path to requirements.txt
    required: false
    default: requirements.txt
  binaries:
    description: Paths to binaries to upload
    required: false
  project:
    description: Name of the existing project for which to retrieve function insights
    required: false
  version:
    description: Version of the project (auto-generated from git if not provided)
    required: false
runs:
  using: node20
  main: dist/index.js
